spring:
  application:
    name: ShoppingApp
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    name: postgres
    password: ----------------
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update

  server:
    port: 8081

